/** Vertical percent at which a falling tile hits the water. */
export const GROUND_Y = 78;

/** Tiles within this many percent of the water turn red. */
export const DANGER_ZONE = 14;

export const MAX_LIVES = 3;

export const MAX_INPUT_LENGTH = 12;

/** How long level/continue banners stay up (gameplay is frozen meanwhile). */
export const BANNER_MS = 1600;

/** Lifetime of clear/splash particles. */
export const PARTICLE_MS = 700;

/** Lifetime of the English-meaning ghost left behind by a cleared word. */
export const GHOST_MS = 1500;

/** Duration of the wrong-input shake on the input display. */
export const SHAKE_MS = 350;

/** Pause on the final splash before showing the game-over screen. */
export const GAME_OVER_DELAY_MS = 900;

/** Delay before the first tile of a level spawns. */
export const FIRST_SPAWN_GAP_MS = 600;
